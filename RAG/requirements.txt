# RAG Workflow API — Python dependencies
# Free / self-hostable stack:
#   Ollama        : LLM + embeddings
#   FAISS         : vector search
#   LangChain     : document loading, chunking, retrieval chain
#   FastAPI       : REST API
#   semcache-api  : semantic cache (separate container, HTTP)

# Vector search
faiss-cpu>=1.8.0
numpy>=1.26.0

# LangChain core + community loaders
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0
langchain-ollama>=0.2.0

# Document loaders
pypdf>=4.0.0
unstructured>=0.14.0

# HTTP (cache client + Ollama health probe)
httpx>=0.27.0

# API layer
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
python-multipart>=0.0.9
